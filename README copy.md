一些编程习惯：
观测
观测代码不能污染业务代码；
意图识别、天气工具这些“业务函数”里不写审计；审计像“管道壁上的感应器”。
但是，有一些结果和异常，如果不在细节函数里面处理，就有可能丢失代码。因此，这些应该即刻进行获取。

业务代码里不再显式拼长 prompt，统一放 Prompt 的位置（app/infra/llm/prompts.py）， system prompt 抽成一个常量/模板放进去。

不要自己设计回退规则，在开发阶段宁愿问题暴露也不要隐藏。